# Hungry Chippos — 8-bit SAR ADC Progress Tracker

> **PDK:** GlobalFoundries 180nm (gf180mcuD) · **Toolchain:** Xschem · Ngspice · KLayout · Netgen · OpenROAD
> **Status legend:** 🟢 Complete · 🟡 In Progress · ⚪ Not Started · 🔴 Blocked

> **Snapshot (2026-07-30).** All three sub-blocks are now individually done:
> **DAC** (Gates 2/3/5, tapeout-ready), **Comparator** (Gate 1 PASS; COMP-10
> `.sch` sync + COMP-11 taps still open), and — as of today — **SAR_LOGIC**
> (SAR-1…6 all closed: the `DFF_SET_N` blocker was an inverted D-capture in
> *both* flops, `sar_logic.sch` had 4 wiring bugs incl. no CLK at all; the
> fixed logic passes a transistor-level binary-search testbench, and the
> full block is laid out at 1219.6×45.7 µm with DRC 0/660 + LVS 458/458).
> Luc's alternative double-tail comparator now lives in its own
> **`comparator_alt/`** folder (COMP-ALT, paper backup — not on the
> critical path).
>
> **Update (2026-07-31): INT-2…INT-8 are ALL closed.** Gates 3, 4 and 5
> all PASS: the ADC is verified end-to-end at transistor level
> (guaranteed input range 0.62–3.29 V across MOS corners) **and the
> full chip block is physically assembled and signed off** —
> `adc_top/layout/adc_chip_top.gds`, DRC 0/660, LVS 862/862
> "Netlists match", density + antenna clean, in padring slot B with 13
> labeled pins. Remaining: padring hookup at chipathon integration,
> spec ratification (ENOB / sample rate), REP-2..5.

---

## 1 · Global Integration (INT)

| ID | Task | Owner | Status | Artifact |
|----|------|-------|--------|----------|
| INT-1 | Repo skeleton, CI stubs, Docker env verified | Bruno | 🟡 In Progress | `README.md`, `.github/workflows/` |
| INT-2 | Define precise block pin interfaces (pin-contract table) | Bruno | 🟢 Complete (2026-07-30) | `docs/pin_contracts.md` — port orders verbatim from the LVS/sim-proven netlists; **conversion scheme decided: DAC as VDAC generator** (DAC VIN pin tied to VSS, SAMPLE = reset-to-0V, ADC input direct to comparator VIN1, DAC_TOP→VIN2, CMP_OUT=VOUT2 polarity via SR NAND latch) — the only full-range scheme with the blocks as built (sampling VIN on the array over-drives the rail at the MSB trial; a mid-rail VIN2 reference carries no VIN information). Glue: CK=~CLK, SAMPLE=~RST_N, SR latch kills the CMP_OUT race. 13 ADC pins. Caveats: low-Vcm window below code ~66 (worst-corner), no input S/H (BW ≈ 1.5 kHz full-scale sine @ 10 MHz CLK). |
| INT-3 | Top-level schematic stitching (ADC top) | Bruno | 🟢 Complete (2026-07-30) | `adc_top/schematic/adc_top.sch` (self-contained `adc_top/` folder, mirroring the block convention) — cap_array + strongarm + sar_logic + glue (CK=~CLK, SAMPLE=~RST_N, buffered NOR SR decision latch). Netlists hierarchically with **zero duplicate subckts** after the dac gate-cell rename (see 2026-07-30 note below). Netlist cwd gotcha: run xschem from `sar_logic/sar_designs` so the SAR's bare-name symbol refs resolve. |
| INT-4 | Top-level symbol for ADC | Bruno | 🟢 Complete (2026-07-30) | `adc_top/schematic/adc_top.sym` — 14 pins, instance pin order verified = `.subckt adc_top VDD VSS VIN CLK RST_N BIT_7..BIT_0 EOC`. |
| INT-5 | Integration testbench (TT corner, full conversion cycle) | Bruno | 🟢 Complete (2026-07-30) | `adc_top/sim/tb_adc_top.spice` + `adc_top_subckt.spice` — first fully transistor-level closed-loop conversions (real DAC + StrongARM + SAR, TT/3.3V/10MHz). 5 conversions: VIN=0.6→46 exact, 1.66→127, 2.9→225 (exact vs measured FS), EOC at 901 ns as specced. **Two real findings:** (1) NAND SR latch state-dependent input cap made near-rail decisions sticky → replaced with inverter-buffered NOR SR latch (see pin_contracts §4); (2) an apparent near-rail decision inversion — **later retracted by INT-6: it was a coarse-timestep simulation artifact**, not physics; at trusted settings the comparator is correct to the top code (see INT-6). VIN=0.05→0 is the real low-Vcm limit. Sim note: this comparator needs `.tran` max step ≤0.05n **and** reltol=1e-4 — coarser settings silently corrupt fine decisions. MIM caps need `.lib sm141064.ngspice mimcap_typical` **and** `cap_mim` sections. |
| INT-6 | **Gate 3** — TT-corner DNL/INL sweep pass (<0.5 LSB) | Bruno | 🟢 **PASS (2026-07-31, over 0.50–3.29 V)** | `adc_top/sim/sweep_tt*/`, `designs/scripts/gen_adc_sweep_tt.py`, `check_adc_sweep.py` — 256-code code-center sweep, 8 parallel chunks. **Attribution complete:** (a) behavioral-comparator sweep (`sweep_tt_bcmp/`): all 256 codes monotonic, exact 0–98, uniform −1 above (= known offset/gain from TG injection + grid's 20 fF load assumption crossing −0.5 LSB at k≈99) → **DAC+SAR+glue formally linear**; (b) standalone comparator with ideal sources: perfect 0.6–3.3 V down to ±3.2 mV (`comparator/sim/comp_vcm_threshold_tt_summary.txt`); (c) ⚠️ **sim-settings hazard:** 0.1n/default-reltol corrupts fine comparator decisions (fake ±2–4 LSB sawtooth + false non-monotonicity); 0.05n + reltol=1e-4 collapses all six rechecked codes to a uniform −1. **Definitive per-code sweep (`sweep_tt_fine2/`, 256 independent constant-VIN runs @ 0.05n/reltol=1e-4, code centers of the measured FS): perfectly monotonic, no missing codes; codes 39–102 exact, 103–255 uniform −1 (offset ≈ −0.5 LSB analog: TG injection + grid-vs-real FS gain), codes 1–38 read 0 (real low-Vcm dead zone).** ⇒ after standard offset/gain correction, DNL/INL within the code-center method's ±0.5 LSB bound over **VIN 0.50–3.29 V** = Gate 3 PASS over that range; input range + offset become datasheet lines. The earlier 'near-rail inversion' is **retracted** (coarse-step artifact — the 0.02n trace and the standalone characterization always showed correct decisions; perfect 0.6–3.3 V down to ±3.2 mV). Measured analog terms: TG injection ≈ +5 mV, kickback −0.3…−0.5 mV/strobe. Deck-generation gotchas: per-code constant-VIN runs (PWL staircase corners abort ngspice at reltol=1e-4: 'trouble with node vvin#branch'); `.save` only the measured signals (else ~3 GB/run allocation failure). |
| INT-7 | **Gate 4** — Full corner sweep (FF / SS / SF / FS) | Bruno | 🟢 **PASS (2026-07-31)** | `adc_top/sim/corners_report.md` + `sweep_{ff,ss,fs,sf}/sweep_codes.csv` — INT-6 methodology per MOS corner (256 per-code runs each, trusted settings, 3.3 V/27 °C, mimcap_typical — global cap variation cancels ratiometrically per DAC-9b). **Every corner structurally identical to TT: monotonic, no missing codes, worst error above the dead zone = 1 LSB (the same calibratable −1 offset band from ~code 103).** Only the low-end dead zone moves, tracking the NMOS corner (FF 0.40 V / FS 0.42 V / TT 0.50 V / SF 0.58 V / SS 0.62 V). ⇒ **guaranteed input range 0.62–3.29 V across all model corners** (quote 0.65–3.25 V with margin). **Supply/temp follow-up closed 2026-07-31** (`adc_top/sim/supply_temp_report.md`, `gen_adc_sweep_vt.py`): reduced 21-code sweeps at 3.0 V, 3.6 V, −40 °C, +125 °C (TT) — every condition structurally identical to nominal (monotonic, same −1 offset band); dead zone tracks NMOS Vth (worst = cold, ≈0.62 V; hot shrinks to ≈0.4 V), so the quoted 0.65 V low bound holds across supply ±10% and full temp. SS×−40 °C cross-corner not run (both axes individually ≈0.62 V — the reason the quoted bound keeps margin over the guaranteed one). |
| INT-8 | **Gate 5** — DRC & LVS clean, tapeout sign-off | Bruno | 🟢 **PASS (2026-07-31)** | `adc_top/layout/adc_chip_top.gds` (topcell `adc_top`, content 30–544 × 0.5–550 µm, fits padring slot B 558.75×1117.5) + generator `designs/scripts/gen_adc_chip_top.py`. Assembles the four proven blocks: DAC `dac_top_floorplan`, StrongARM (dbu-0.005 GDS rescaled), **3-row folded SAR** (`sar_logic/layout/sar_folded.gds`, 457.9×130.2 µm, `gen_sar_layout.py --fold 3`, standalone DRC 0/660 + LVS 458/458, mirrored so the fold-port column faces the west channel), and **adc_glue** (`adc_top/layout/adc_glue.gds`, the 6 glue gates, DRC 0/660 + LVS 16/16). Table-driven routing with 3 in-script checkers (attach probes, route-vs-block clearance, cross-net clearance); 13 labeled M5 pins on the south edge. **Sign-off on the final GDS (variant=D):** main DRC **0/660**; LVS vs `adc_top/layout/refs/adc_top_ref.spice` (generated native M/C reference, `gen_adc_chip_ref.py`, lvs_sub=VSS) **"Netlists match" — 862 devices** (377 DAC + 11 comparator + 458 SAR + 16 glue); `--density_only` clean (dt-4 dummy fill added: COMP 57/poly 57/M1 56/M2 35/M3 36/M4 39/M5 41.5%, DAC bbox excluded from metal fill); `--antenna_only` clean. COMP-11 taps closed here (11 substrate taps under the VSS band branch). Notable finds: MetalTop@11K rules (MT.1 0.44/MT.2a 0.46) forbid M5 at the 0.8 µm fold-port pitch; one pre-existing marginal via1 enclosure in `strongarm.gds` (its dbu-0.005 DRC quantized away the rule's 2 nm epsilon) patched at chip level; glue pad order is a chip-level routing contract (lane-ordering rules at both ends must agree). |

---

## 2 · Comparator Block (COMP)

| ID | Task | Owner | Status | Artifact |
|----|------|-------|--------|----------|
| COMP-1 | StrongARM latch schematic entry | Bruno | 🟢 Complete | `comparator/schematic/strongarm.sch` |
| COMP-2 | Comparator symbol creation | Bruno | 🟢 Complete | `comparator/schematic/strongarm.sym` |
| COMP-3 | Functional transient testbench (single-shot) | Bruno | 🟢 Complete | `comparator/schematic/strongarm_tb.sch` |
| COMP-4 | Monte Carlo testbench setup (N ≥ 100 runs) | Bruno | 🟢 Complete | `comparator/schematic/strongarm_mc_tb.sch` |
| COMP-5 | **Gate 1** — input-referred offset characterized (MC) + delay < 2 ns | Bruno | 🟢 **PASS (2026-07-19)** | `comparator/comp_mc_report.txt`, `comp_mc_offsets.{raw,txt}` — **Offset status (separate):** the +15.1 mV systematic was a `#net5`/M6 wiring bug, now **FIXED**. MC re-run at real silicon mismatch (svt ≈ 24.8 mV, N=100, good=100/100): **mean +2.6 mV (≈ 0, no systematic), σ = 36.9 mV** (matches predicted √2·svt ≈ 35 mV). Note: literal "σ < 2 mV" is a flash/pipeline target — for a SAR, comparator offset is a calibratable **DC shift** with no DNL/INL impact → **offset sub-gate cleared, no device upsizing**. **Delay sub-item PASS:** fresh LVS-clean device-level extraction, 10 fF/output, SS/125 C/2.97 V/Vcm=1.485 V gives **362.078 ps @ 0.5 LSB** and **370.455 ps @ 0.1 mV** (5.40x margin to 2 ns); TT/27 C/3.3 V reference = 201.855 ps. Metastability: delay *saturates* at ~370.6 ps down to 0.1 µV overdrive — no metastable tail within spec. Vcm window: compliant for **Vcm ≥ 0.85 V**, hard fail ≤ 0.6 V (mid-rail SAR operating point is comfortably inside). See `comparator/sim/comp_delay_postlayout_{ss_2v97,tt}.txt` and `comparator/WORKLOG.md`. **Both sub-gates now closed → Gate 1 PASS.** ⚠️ Caveat carried to COMP-9: `run_lvs.py` yields a *device-level* deck (extracted W/L + topology, **no interconnect R/C**), so this is not a full-RC PEX claim — routing parasitics still to be confirmed at PEX. |
| COMP-6 | Comparator physical layout (KLayout) | Bruno | 🟢 Complete | `comparator/layout/strongarm.gds` |
| COMP-7 | Sub-block DRC clean | Bruno | 🟢 Complete | `comparator/layout/backups/strongarm_DRC_CLEAN_2026-07-10_15h26m1783711590.gds` — 0 violations (antenna clean; density flags only whole-cell minimum-density, deferred to chip-level dummy fill) |
| COMP-8 | Sub-block LVS clean (KLayout, not Netgen) | Bruno | 🟢 Complete | `comparator/layout/backups/strongarm_LVS_CLEAN_2026-07-10_10h44m1783694640.gds` — netlists match, 11/11 devices correct. **Note:** verify via the terminal `run_lvs.py` flow in `handoff/README.md`, not the KLayout GUI's "Run KLayout LVS" menu action — that GUI path has a confirmed bug (false short) on this layout, tracked in `bugs/github-issue.md` |
| COMP-9 | Post-layout extraction (PEX) corner simulation | TBD | 🟡 Partial | `comparator/layout/strongarm_extracted_clean.spice` — device-level extracted deck (LVS-clean GDS, 11 devices) exists and was used for the COMP-5 delay sign-off. **Missing: true parasitic R/C extraction.** Magic ext2spice attempted 2026-07-31 (`comparator/layout/pex/`): the gf180mcuD magic tech mis-binds this hand-drawn GDS's nets (34/10 labels not read as ports, nets merge into VSS) — needs real tech-setup work, not pursued. Bounding evidence from the partial extraction: every interconnect coupling cap magic did report is 0.01–0.3 fF, ≪ the 10 fF/output load already assumed in the COMP-5 sign-off; wire R at this 83 µm scale is tens of Ω vs kΩ device Ron. With the 5.4× delay margin, PEX remains confirmation, not risk. |
| COMP-10 | Sync `strongarm.sch` to the LVS-proven netlist | Bruno | 🟢 Complete (2026-07-30) | `comparator/schematic/strongarm.sch` — fixed the unit-less `M1 L=0.28` typo (→ `0.28u`) and replaced all five NMOS source-tied bulk U-loops (M1→net1, M8/M10/M11→net2, M9→net3 — unbuildable without deep-nwell) with `lab_wire` VSS ties at the bulk pins. **Verified by re-netlisting headlessly and diffing device lines against `strongarm.spice`: exact match** (topology, W/L, bulks, port order `VDD CK VOUT2 VOUT1 VIN1 VIN2 VSS`). The schematic is now safe to instantiate at ADC top level. |
| COMP-11 | Latch-up hardening — additional substrate taps | Bruno | 🟢 Complete (2026-07-31, at chip level) | `adc_top/layout/adc_chip_top.gds` — 11 p-substrate taps (copies of strongarm's own proven tap stack) in a row under the chip's M5 VSS band branch at y 174, x 350–500 µm, spanning the glue + comparator region; each tied into the branch by a via1–via4 stack. Verified inside the INT-8 chip DRC 0/660 + LVS. The chip-level dummy fill (COMP-7 note) also closed in INT-8. |

---

## 3 · Capacitor DAC Block (DAC)

> **Interface note (2026-07-10, from Sam's SAR design doc):** the SAR_LOGIC top-level's bottom row is 8 OR gates (NOR2+INV), one per bit, each driving one DAC switch based on the sequencer + code-register state. The DAC exposes **8 single-ended digital switch-control pins** (one per cap) — feeds into the INT-2 pin-contract table too.

> **Relocation note (2026-07-17):** the DAC now lives in a **self-contained top-level `dac/` folder** (`dac/schematic`, `dac/sim`, `dac/layout`), mirroring `comparator/`. The old `designs/libs/core_cap_dac/` and `designs/libs/tb_cap_dac/` scratch copies were deleted after the files were ported and verified. Running notes live in `dac/WORKLOG.md`.

| ID | Task | Owner | Status | Artifact |
|----|------|-------|--------|----------|
| DAC-1 | Binary-weighted 8-bit cap array schematic (C_u ≥ 50 fF) | Bruno | 🟢 Complete | `dac/schematic/cap_array.sch` — 8-row binary-weighted bottom-plate array (C_u = 50 fF, weights 1–128). **Connectivity-verified in Xschem** (all 8 caps share `DAC_TOP`, no floating ports). ⚠️ The original hand-authored version had **cosmetic-only `lab=` text that netlisted as a fully disconnected array** — real `lab_wire` components were added and the netlist re-verified. Includes per-bit switch sizing (DAC-3b) + SAMPLE-gating (DAC-4b). Input sampling front-end being finalized as top-plate sampling (see SWITCH-4). |
| DAC-2 | Cap array symbol | Bruno | 🟢 Complete | `dac/schematic/cap_array.sym` — 12-pin (VIN, VDD, SAMPLE, B0–B7, DAC_TOP). `VREF` pin removed 2026-07-18 (was 13-pin incl. VREF) — see DAC-4/DAC-9 VREF=VDD rework. |
| DAC-3 | 256×C_u switching & settling testbench | Bruno | 🟢 Complete | `dac/sim/tb_major_carry.sch` — major-carry (0111_1111→1000_0000) Gate-2 settling tb, connectivity-verified. Supersedes the old `tb_cap_array.sch` (single-bit, electrically disconnected — **deleted 2026-07-17**). |
| DAC-3b | Unit-cell switch — CMOS rail driver (VREF=VDD rework) | Bruno | 🟢 Complete | `dac/schematic/unit_switch.sch`/`.sym` — **redesigned 2026-07-18**: was a 2-NMOS pass-gate to `VREF`(1.65V)/GND (no bootstrap; originally authored by Max on `origin/max`, ported + per-bit sized W=2ⁱ×0.42µm); an NMOS pass transistor can't pull to the full `VDD` rail, so VREF was capped below `VDD`. Now a CMOS rail driver: PMOS pull-up to `VDD` + NMOS pull-down to `GND`, both gated by the single `bN_bar=NAND2(Bn,SAMPLE_N)` signal (the old separate `bN` AND-inverter stage was removed as dead logic once both gates needed the same signal). `pfet_wid=2×nfet_wid` per bit (inv1/nand2/tgate P:N=2:1 convention); MSB PMOS (107.52µm) exceeds gf180mcuD's binned-model width ceiling (100.001µm) so it's built as `pfet_wid=53.76u pfet_m=2`. A single unit-sized switch (pre-DAC-3b) missed MSB settling by ~112× (276 ns); scaling W with cap weight holds τ constant → Gate 2 PASS. |
| DAC-4 | **Gate 2** — 0.5 LSB settling within 40 ns (TT + PVT corners) | Bruno | 🟢 **PASS** | `dac/WORKLOG.md`, `VERIFICATION_PLAN.md` — post VREF=VDD rework (2026-07-18, LSB now 12.9mV/FS=3.3V): TT settle=2.61ns err@40n=0mV; worst corner **SS/125°C/2.97V settles in 3.86 ns (36.1 ns margin)**, err@40ns=0mV. (Previously, under VREF=1.65V: TT 1.77ns, full 30-corner sweep worst case SS/125°C/2.97V 2.78ns — superseded.) |
| DAC-4b | SAMPLE-gating — remove sampling-phase switch contention | Bruno | 🟢 Complete | `dac/schematic/cap_array.sch` + new `nand2`/`nor2` cells — bit drivers gated by SAMPLE so both switch arms open during sampling. Crowbar current cut from ~4–10 mA to pA; Gate-2 regression still PASS. |
| DAC-4c | Sample-and-hold characterization (acquisition, hold droop, kT/C) | Bruno | 🟢 Complete | `dac/sim/tb_sample_hold.sch`, `dac/WORKLOG.md` — full-range acquisition now PASSES with the TG top switch (3.0 V settles 78.6 ns worst-corner, 3.2 V 73.7 ns, sub-mV error). Hold droop negligible; kT/C ≈ 18 µV rms. Testbench needed a `.ic`/`uic` fix (was pre-charging DAC_TOP via a DC-op-point artifact). |
| DAC-4d | Support cells + top-plate sample switch | Bruno | 🟢 Complete | `dac/schematic/{inv1,nand2,nor2,tgate}.sch` — TG top-plate switch sized nfet 4 µm / pfet 8 µm (L = 0.28 µm), Ron ≈ 294–1098 Ω. Sizing chosen to cap charge injection at 0.18 LSB (vs 0.26 LSB for a faster 6/12 µm option). |
| DAC-9 | 256-code nominal INL/DNL transfer sweep (DAC-only, TT) | Bruno | 🟢 Complete | `dac/sim/tb_inl_dnl.sch`, `designs/scripts/extract_dnl_inl.py`, `dac/docs/figures/{dnl,inl}_vs_code.png` — one 64 µs stepped transient, fixed VIN=0 sample input. **Re-verified 2026-07-18 after VREF=VDD rework:** measured FS span = 3292.98 mV (99.8% of the 3.3V rail — gain gap resolved, was ≈1.6465V≈VREF/2× short before the rework), V_LSB=12.914mV. Nominal (perfectly-matched schematic caps): max\|DNL\|=0.00216 LSB (code 196), max\|INL\|=0.00439 LSB endpoint / 0.00290 LSB best-fit — monotonic, no missing codes. |
| DAC-9b | Capacitor-mismatch Monte Carlo INL/DNL (the real linearity limit) | Bruno | 🟢 **PASS @ Cu=50fF** | `designs/scripts/dac_mismatch_mc.py`, `dac_mismatch_mc_spice.py`, `dac/docs/figures/mc_*.png` — **corrected 2026-07-17 (Step 2e):** earlier verdict (71.5% yield, Cu≥200-400fF needed) used the PDK's *global* `cap_mim` MC term (`sw_stat_global`, 2.5%) as if it were independent *local* per-cap mismatch — a modeling bug, not a real result (global variation is a common scale factor and cancels in this ratiometric DAC; only local mismatch drives DNL/INL). Redone with a proper local Pelgrom model, `sigma(C_unit)/C_unit = A_C/sqrt(Area)` (A_C=1.6%·µm primary, 3.2%·µm conservative 2× — literature 180nm-MiM estimates, gf180mcuD ships no local number). Result at N=300,000: yield **≥99.9997%** at Cu=50fF for both A_C cases (worst\|DNL\|=0.25-0.51 LSB, worst\|INL\|=0.13-0.28 LSB, both < 0.5 LSB spec). **No unit-cap upsizing required — current 50fF/5µm×5µm is adequate.** Global 2.5% effect isolated separately: full-scale gain error ≈0.004% sigma (not 2.5% — cancels almost entirely in the cap ratio), zero DNL/INL impact, confirming the local/global distinction. See `dac/WORKLOG.md` Step 2e. |
| DAC-5 | Unit-cell layout with common-centroid placement (KLayout) | TBD | 🟡 Partial | `dac/layout/cu_cell.gds` — 50fF cap_mim unit cell redrawn for **variant=D (metal_top=11K, mim_option=B, metal_level=5LM — the real, signed-off chip stack)**: FuseTop top plate over a Metal4 bottom plate, Via4 breakouts to two symmetric (N+S) Metal5 pads, 6.2×8.06 µm footprint, symmetric about both axes. DRC clean (0 violations, 660 rule categories, `run_dac_drc.sh`) and LVS clean (`run_dac_lvs.sh`, native SPICE `C`-element reference netlist, extracts as `cap_mim_2f0fF`/`CAP_MIM_2F0FF` @ 50fF, device count 1 on both sides, "Netlists match"). Supersedes the earlier variant=A/3LM cell (commit `15e9d45`), which extracted as 0 devices under the real stack — see `dac/WORKLOG.md` 2026-07-18 entries. **Per DAC-9b (corrected), Cu=50fF is adequate for random mismatch; no upsizing needed. Common-centroid placement is still mandatory** (cancels *systematic* oxide-gradient error) — the 255-unit array + common-centroid tiling (Step 2) has **not** started; this entry covers the single-cell checkpoint only. |
| DAC-6 | Full array layout with dummy/fringe peripheral caps | TBD | 🟢 Complete | `dac/layout/cap_array.gds` — routed 255-unit common-centroid array with all active top plates on the balanced Metal5 `DAC_TOP` mesh; B0–B7 bottom-plate rails; and both plates of all 69 dummies tied to GND. |
| DAC-7 | Sub-block DRC clean | TBD | 🟢 Complete | `run_dac_drc.sh` on routed `cap_array.gds`, variant=D: **0 violations**, 660 rule categories, full untruncated run. |
| DAC-8 | Sub-block LVS clean (caps-only) | TBD | 🟢 Complete | `run_dac_lvs.sh`, variant=D, against `dac/layout/cap_array_caps_only_ref.spice` (native C-element reference): **Netlists match**, 324 layout and 324 reference devices. Full array+switch LVS remains deferred until switch layout. |
| DAC-10 | Full DAC floorplan + placement (pre-routing) | Bruno | 🟢 Complete | `dac/layout/dac_top_floorplan.gds`, `designs/scripts/gen_dac_top_floorplan.py` — hierarchical cap-array core, 8 NAND2/driver columns y-aligned to their Metal2 bit-rail exits, and right-side SAMPLE_N inverter/TG placement. **No routing yet.** Variant-D placement DRC: **0 violations / 660 rule categories**; full-block LVS deferred until routing. |
| DAC-10a | Top-level power + signal routing (all 8 bits) | Bruno | 🟢 Complete | All eight per-bit chains are routed. The via-cut checker confirms every bit’s four signal paths and VDD/0 drops, **27 tracked intended-distinct nets pairwise separate (0 merges)**, and pristine `cap_array` has **8 distinct rails**. Variant-D full-block DRC: **0 violations / 660 categories**. Real-reference LVS remains a separate Gate-5 task. |

---

## 4 · SAR Digital Controller (SAR)

> **Design-flow update (2026-07-11):** the controller is built as **full-custom schematic capture in xschem** (gate-level, Anderson architecture) rather than the Verilog → Yosys → OpenROAD digital-synthesis flow originally planned below. **Correction:** this was previously noted as living only in Sam's unmerged fork — PR #3 (`sar`) merged into `main` on 2026-07-11, so the cells and top-level schematic now live in this repo at `sar_logic/sar_designs/`. `sar_logic/{rtl,syn,pnr}` still only contain `.gitkeep` placeholders from the old synthesis-flow plan.

| ID | Task | Owner | Status | Artifact |
|----|------|-------|--------|----------|
| SAR-1 | Standard-cell schematic entry + testbench: INV, TG, NAND2, NOR2, DFF_RST_N, DFF_SET_N | Sam / Bruno | 🟢 Complete (2026-07-30) | `sar_logic/sar_designs/*.sch` — the "DFF_SET_N bug" turned out to be **an inverted D-capture in BOTH flops** (slave TG tapped the NOR/NAND output — one inversion from master storage — giving 3 inversions D→Q, so Q=NOT(D) on every edge; async set/reset behavior was correct, which is why `dff_rst_n` "passed" its tb — only reset was ever eyeballed, and `tb_dff_set_n` held D=0 constant). Fixed by retapping the slave input TG from the x7 inverter output in both `dff_set_n.sch` and `dff_rst_n.sch`; also fixed a W/L swap on the `tg.sch` PMOS (was L=1u/W=0.28u). ngspice edge-by-edge checker: Q==D at every rising edge for both flops, async SET→1 / RST→0 verified. `tb_dff_set_n` now pulses D. |
| SAR-2 | Top-level SAR_LOGIC schematic — Anderson architecture: 9-FF sequencer + 8-FF code register + 8× (NOR2+INV) OR-gate DAC-drive stage, 17 flip-flops total | Sam / Bruno | 🟢 Complete (2026-07-30) | `sar_logic/sar_designs/sar_logic.sch` — architecture was assembled correctly but had **4 wiring bugs** found by netlisting: (1) **no CLK at all** — the clock rail terminated on S6's VSS drop, tying all 9 sequencer clocks to VSS, and no CLK input pin existed (added, now the 14th port); (2) S2.Q **shorted to VDD** (C2's VDD drop wire ran exactly through the Q pin); (3) C5.Q **shorted to VSS** (C5's VSS drop overlapped the Q riser); (4) x13's VSS pin **floating** (stub overshot the ground rail by 10 units). FF instances also renamed `S*`/`C*`→`XS*`/`XC*` (S/C prefixes netlist as SPICE switch/cap elements). |
| SAR-3 | Full binary-search functional testbench (verify 8-bit code capture + End-of-Conversion signal) | Bruno | 🟢 **PASS (2026-07-30, TT)** | `sar_logic/sim/tb_sar_logic.spice` (SPICE deck, headless-reproducible; supersedes the planned `.sch` tb) — transistor-level `sar_logic` (gf180mcuD typical, 3.3 V, 10 MHz CLK) closed in a loop with behavioral B-source DAC + comparator (1 ns RC on CMP_OUT, conservative direction for the C_i latch race). **Five full conversions: VIN = 0.05/1.66/0.6/2.9/3.25 V → codes 3/128/46/224/252, all = floor(VIN·256/3.3)**; EOC rises 8 CLK edges after RST_N release (~901 ns) and C0 latches the trial-0 decision on it; code held after the EOC pulse. Checker: `check_sar.py` (scratchpad; decodes bits at EOC+40 ns). |
| SAR-4 | SAR_LOGIC physical layout (KLayout) | Bruno | 🟢 Complete (2026-07-30) | `sar_logic/layout/sar_cells.gds` (topcell `sar_logic`, **1219.6 × 45.7 µm**), generator `designs/scripts/gen_sar_layout.py`. Three-level hierarchy: planar poly+M1 leaf cells (inv/tg/nand2/nor2, PDK PCell devices) → DFF row assemblies → 34-instance top row (9 seq DFFs + 8 code DFFs + 8×(nor2+inv)). Generic channel router: one exclusive M2 track per net + M3 drop verticals (0.8 µm greedy pitch); DFF pins exported as fixed-x M3 pads. Single-row strip — fold into multiple rows later if the chip floorplan needs a squarer block. |
| SAR-5 | Sub-block DRC clean | Bruno | 🟢 Complete (2026-07-30) | `run_dac_drc.sh` variant=D on every level: 4 leaf cells, both DFFs, and topcell `sar_logic` all **0 violations / 660 rule categories**. Earned rules recorded in `gen_sar_layout.py` docstrings (gate-pad via landings 0.26×0.60 µm per V1.3a; V2.3c/d via2 pads; 0.65 µm track pitch per M2.2a). |
| SAR-6 | Sub-block LVS clean | Bruno | 🟢 Complete (2026-07-30) | `run_dac_lvs.sh` variant=D vs native-element references in `sar_logic/layout/refs/` (hierarchical, mirrors the simulation-verified xschem netlist): leaf cells, both DFFs, and `sar_logic` all **"Netlists match"** — top level extracts **458 MOS = exact schematic device count**. ⚠️ Leaf gotcha for future cells: a poly gate-join stripe wider than the 0.28 µm gate poly extracts as L=0.3u (LVS catches it, DRC does not). |

---

## 5 · Bootstrap Switch / Sample-and-Hold (SWITCH)

| ID | Task | Owner | Status | Artifact |
|----|------|-------|--------|----------|
| SWITCH-1 | Bootstrapped switch schematic (S/H front end, constant V_GS ≈ 3.3 V) | Emily | 🟢 Complete (concept) | `designs/emily_testing/TB_bootstrap_switch.sch`. **Evaluated by us (2026-07-17)** as the DAC's full-range top-plate sample switch and found unusable as built — its clock-boost inverter (`CLK_INV`) is powered from `VIN` instead of `VDD`, so the bootstrap gate overdrive collapses as VIN→VDD (fails above ~2.8 V). Fine as a demo, but not usable for full-range sampling without a power-rail fix. |
| SWITCH-2 | Bootstrap switch testbench (ideal-inverter clock, transient) | Emily | 🟢 Complete | same file — tracks V_IN at low/mid input; see SWITCH-1 note for the near-rail limitation. |
| SWITCH-3 | 3-way NMOS switch submodule (sample / ref / ground) | Mimi *(file committed by Emily — confirm ownership)* | 🟢 Complete | `designs/emily_testing/TB_swtich2.sch`. ⚠️ `switch3.sch` (same folder) is an empty stub — not this. |
| SWITCH-4 | DAC sample switch — architecture + integration | Bruno | 🟢 Complete | **Top-plate sampling** integrated (commit 4029408): single full-range **sized transmission gate** (nfet 4 µm / pfet 8 µm) on `DAC_TOP`, gated by `SAMPLE`/`SAMPLE_N`; bottom plates toggle VDD/GND (was VREF/GND before the 2026-07-18 VREF=VDD rework, see DAC-3b). Connectivity verified (8 caps on `DAC_TOP`, no floats). |
| SWITCH-5 | S/H settling sim with worst-case DAC load + comparator C_in | Bruno | 🟢 Complete | `dac/sim/tb_sample_hold.sch` — full 12.77 pF array + 20 fF comparator cap. Full-range acquisition PASS (0.3 / 1.65 / 3.0 / 3.2 V), worst-corner error <0.02 mV. Charge injection ≤0.18 LSB — largest single error term, watch in INL/DNL. |
| SWITCH-6 | `unit_switch` CMOS rail driver layout — all bit sizes | Bruno | 🟢 Complete | `dac/layout/unit_switch_checkpoint.gds`, generator `designs/scripts/gen_dac_switch_layout.py`. Bits 0–7 use exact schematic W/L; bit7 retains physical PMOS multiplicity 2. **Bits 1–6:** DRC 0 violations (variant D) and native-SPICE LVS match, 2 layout/2 reference devices each. |
| SWITCH-7 | Per-bit control logic (`inv1`, `nand2`) layout | Bruno | 🟢 Complete | Same generator, topcells `inv1`/`nand2` in `dac/layout/dac_logic_checkpoint.gds`. `inv1`: DRC/LVS clean (2 devices). `nand2`: planar widened M1 layout is DRC clean (**0 violations, 660 variant-D rule categories**) and native-element LVS clean (**4 devices each side; Netlists match**); A/B/Y remain distinct. **Switch-cell layout Step 1 closed.** |
| SWITCH-8 | Top-plate TG layout | Bruno | 🟢 Complete | `dac/layout/dac_logic_checkpoint.gds` topcell `tgate`, native reference `dac/layout/tgate_ref.spice`: 4µm NMOS / 8µm PMOS, L=0.28µm, correct complementary gates and body ties. Variant-D DRC 0 violations; LVS match, 2 devices each side. |

**Note on Mimi:** her `mimi-test` branch has no commits beyond `main` — worth checking whether she has unpushed work or this was a joint/miscredited item.

**Architecture question — RESOLVED + VERIFIED (2026-07-17):** the DAC uses **top-plate sampling with a sized transmission gate** as the single full-range sample switch. Silicon-realistic worst-load simulation now passes full-range acquisition; the bottom-plate-only NMOS scheme and Emily's bootstrap switch both failed near the rail (~2.8 V ceiling), so neither is used for full-range sampling. See `docs/adc_glossary.md`.

---

## 6 · Alternative Comparator Design (COMP-ALT)

> **Design-direction note (2026-07-28):** this block takes the *opposite* approach to COMP-5 on offset. COMP-5 treats comparator offset as a calibratable DC shift and does no upsizing; COMP-ALT reduces it in the analog domain with a **double-tail** topology (clocked dynamic preamp → StrongARM latch, Schinkel ISSCC 2007). Both are valid for a SAR — this one is the backup that also removes kickback into the cap-DAC top plate, which a bare StrongARM injects directly onto the node being measured. Full reasoning, verification history and tool/PDK gotchas are recorded in **[`DESIGN_LOG.md`](DESIGN_LOG.md)**.
>
> **Update (2026-07-31):** the post-LVS schematic has been re-validated by Monte Carlo — **σ_offset = 1.30 mV at N=100**, closing both blockers COMP-ALT-7 previously carried. See COMP-ALT-7 for the caveat that makes this *Run A* rather than the final reportable number.

| ID | Task | Owner | Status | Artifact |
|----|------|-------|--------|----------|
| COMP-ALT-1 | Alternative comparator topology (backup/comparison to the StrongARM latch) | Luc | 🟢 Complete | `DESIGN_LOG.md` — **double-tail** selected (dynamic preamp + StrongARM latch). Baseline StrongARM MC gave mean +2.6 mV, **σ = 36.9 mV** (N=100), matching the predicted √2·svt ≈ 35 mV → offset is **input-pair-Vth-dominated**. `A_VT ≈ 6.2 mV·µm` extracted from our own data (24.8 mV × √(0.22×0.28 µm²)) rather than assumed. Pelgrom's √area law then shows a direct-upsizing fix needs **~340× gate area** on the latch's speed-critical nodes — rejected, since those same devices set both the regeneration time constant and the comparator's input cap (which the DAC has to drive). A gain stage divides the latch's offset by A instead, moving the area burden onto a **speed-uncritical** preamp input pair. Dynamic (clocked-tail, capacitively-loaded) preamp chosen over a static one for **zero static current** — a static bias would burn current through all 8 bit-trials for a block needed ~100s of ps per trial. |
| COMP-ALT-2 | Dynamic preamp schematic + symbol (`preamp_dyn`) | Luc | 🟢 Complete | `comparator_alt/schematic/preamp_dyn.sch`/`.sym` — input pair sized **by gate area**, not by W/L directly: 1.1 mV input-referred budget → σ_Vth ≈ 0.78 mV/device → W·L = (6.2/0.78)² ≈ **64 µm²**, drawn as **W=4 µm, L=1 µm, m=16** (L=1 µm both for output resistance and because `L=2u` trips a gf180 BSIM binning error, "u0 not positive"; m=16 rather than one 64 µm device so the v2 common-centroid array drops straight in). Precharge pfets 3 µm/0.3 µm. **Tail retuned 2026-08-04 (W=2µm/L=1µm m=1) and again 2026-08-05 → W=1µm/L=1µm m=1** (halved to double every corner's CKL window — see COMP-ALT-10) (was 2µm/0.4µm ×5 — ⚠️ the COMP-ALT-8 nf→m edit is *not* electrically neutral in ngspice: `m` multiplies total width, `nf` doesn't; it made the pair ×16 — intended, offset budget needs 64µm² — but the tail ×5 by accident, collapsing the integration window ~10×; see `comparator/WORKLOG.md` 2026-08-04). |
| COMP-ALT-3 | Standalone preamp gain testbench | Luc | 🟢 Complete | `comparator_alt/schematic/tb_preamp.sch` — **A_v ≈ 6.1** (peak differential 6.08 mV at t=4.705 ns for 1 mV in, 30 fF load). First measurement read 0.586 and was a **timing artifact**: a dynamic preamp's differential output *peaks and then collapses* as both output nodes bottom out, so the `.meas` has to find the peak, not sample a fixed time. |
| COMP-ALT-4 | Two-stage comparator schematic + symbol (`comparator_2stage`) | Luc | 🟢 Complete | `comparator_alt/schematic/comparator_2stage.sch`/`.sym` — preamp DIP1/DIP2 → latch VIN1/VIN2. Three integration fixes, each earned by a diagnosed failure: **(a)** latch input pair XM8/XM10 enlarged to W=8 µm/L=1 µm (≈8 µm², vs the 0.06 µm² minimum) so its divided-down contribution sits under the preamp's floor — 130× area, affordable only because the preamp does the rest; **(b)** **two-phase clock**, latch clock CKL delayed to fire at the preamp's differential peak; **(c)** precharge PMOS XM3–XM6 widened to **W=5 µm** — the keystone fix for a hard "**VOUT2 never resets**" stuck-latch failure. (c) was *caused* by (a): the enlarged pair added node capacitance the minimum-width precharge devices could no longer discharge within the clock-low phase, so the latch began each evaluation from a corrupted initial condition. **In any dynamic circuit the reset path is a first-class timing constraint.** |
| COMP-ALT-5 | Two-stage Monte Carlo mismatch testbench (N ≥ 100) | Luc | 🟢 Complete | `comparator_alt/schematic/comparator_2stage_mc_tb.sch` — explicit per-device `delvto` injection with sigmas derived from Pelgrom and each pair's actual area (0.78 mV preamp, 3.58 mV latch), with the PDK's own statistics disabled (`sw_stat_mismatch=0`, `sw_stat_global=0`) so they don't double-count. Deliberately transparent: every injected sigma is a number we derived and can defend in review. |
| COMP-ALT-6 | CKL timing sweep (integration-gain tuning) | Luc | 🟢 Complete (re-swept 2026-08-04) | Gain in a dynamic preamp is `A ≈ (g_m/C_L)·t_int` — set in the **clock domain**, so it tunes without re-layout. **Old result (3.3ns → σ=1.69mV) superseded and retroactively invalid:** after the nf→m edit the preamp collapsed in ~0.4ns, and every CKL ≥ 2.7n "measurement" was a **reset-overlap artifact** (CK fell while CKL was still high; the preamp precharge snap made the latch decide from residue). After the tail retune + CKL width 8n→5n: **flat window σ=0.92–0.96 mV across CKL=2.6–3.0n** (N=30/pt, paired seed 12345), hard cliff ≥3.1n. CKL=2.8n was chosen for two-sided margin at mid-rail; **retuned to 3.1n on 2026-08-05** after the ALT-11 realistic-drive screen exposed an ss/3.0V low-input wrong-polarity failure at 2.8n — measured global window (2.9, 3.25) ns, see COMP-ALT-11. Lesson upgraded: re-sweep CKL after any sizing change *and* re-verify with single-shot waveforms — MC statistics can look plausible while measuring an artifact. See `comparator/WORKLOG.md` 2026-08-04. |
| COMP-ALT-7 | **Gate 1 (alt)** — input-referred offset characterized (MC) | Luc | 🟢 **Complete (offset PASS, 2026-08-04)** | **N=200, good=200/200, CKL=3.1n: mean = −0.12 mV, σ = 1.14 mV** (sign-off run 2026-08-05; N=100 runs at 2.8n/3.1n gave 1.13/1.10 — all consistent). Worst-corner bound: **ss/125°C/3.0V, N=100: σ = 1.29 mV** (36% margin even there, see COMP-ALT-11) — **33× below** the 36.9 mV StrongARM baseline, 44% margin to the 2 mV target. Distribution is clean Gaussian (72/96/100% within 1/2/3σ), zero misses, zero endpoint artifacts. Covers both caveats that previously blocked this row: run on the **resized latch** (W=2µm/0.42µm cross-coupled devices) **with VSS bulk ties** (COMP-ALT-9 body effect included), plus the corrected preamp tail and a swept-and-confirmed CKL. Artifacts: `comparator_alt/results/mc_ckl2p8_n100_{report,offsets}.txt`. Prior results superseded: 1.44 mV (pre-resize sizes) and the 07-31 1.30 mV run (artifact-suspect, see COMP-ALT-6). Decision completes ~1.0 ns after CK edge (TT, −10 mV od, informal) — delay corners now formally closed — see COMP-ALT-10. *(Referenced `DESIGN_LOG.md` §9.1–9.2 is missing from the repo — see WORKLOG note.)* |
| COMP-ALT-8 | `preamp_dyn` physical layout — DRC + LVS clean | Luc | 🟢 Complete | `comparator_alt/layout/preamp_dyn.gds` — 0 DRC violations; KLayout LVS netlists match. Two lessons cost real time: **KLayout honors `m` and silently drops `nf`**, so a schematic `W=4u nf=16` read as 4 µm against an extracted 64 µm (rule: **W × m must equal total drawn width, `nf` is decoration** — emit `nf=1 m=16`). And a wave of OFFGRID errors traced not to careless drawing but to KLayout's **"snap to objects"** overriding the 0.005 µm grid snap; fixed with Select All → Edit → Selection → Snap to Grid @ 0.005. |
| COMP-ALT-9 | `strongarm_2` physical layout — DRC + LVS clean | Luc | 🟢 Complete | `comparator_alt/layout/strongarm_2.gds` + reference `strongarm_2.spice` — 0 DRC violations; LVS all 7 pins, 10 nets (incl. the 3 internal nodes) and all devices matched. Four distinct failures were worked through: a **stale cached top-cell name** (comparing `preamp_dyn` against `strongarm_2.gds`); a total device-correspondence collapse caused by **source-to-bulk ties shorting net1/net2/net3 into the shared p-substrate**; a Metal1 gap found by **net counting** (more layout nets than reference = open, fewer = short) rather than by eye; and a "missing PMOS" that was `combine_devices` correctly merging two genuinely-parallel devices. Schematic NMOS bulks were retied source→VSS to match the shared substrate (gf180mcuD has no deep-nwell isolation here) — this adds **body effect** on the latch input pair, now reflected in the COMP-ALT-7 re-run. |
| COMP-ALT-10 | Delay measurement (< 2 ns) at TT + worst corner | Luc | 🟢 **Complete (PASS, re-certified at CKL=3.1n 2026-08-05)** | `comparator_alt/sim/run_comp2_delay_corners.sh`, `comparator_alt/sim/comp2_delay/` — CK-edge→decision at the CKL=3.1n design point (od=0.5LSB / 0.1mV): **TT 1.32/1.38 ns · SS/125°C/2.97V 1.52/1.58 ns (binding, 21% margin) · FF/−40°C/3.63V 1.29/1.36 ns**; probes at 2.9/3.3n resolve everywhere. Budget split ≈1.05 ns strobe wait (buying 32× offset) + ≈0.5 ns latch regeneration; vs COMP-5's 362 ps. History: first pass found the FF cliff below a 2.8n strobe → preamp tail halved to W=1µm (COMP-ALT-2); ALT-11 then moved the design point 2.8→3.1n. Schematic-level; PEX re-map at COMP-ALT-14. See `comparator/WORKLOG.md` 2026-08-05. |
| COMP-ALT-11 | Corner + supply + common-mode sweeps, MC at N ≥ 200 | Luc | 🟢 **Complete (2026-08-05)** | `comparator_alt/sim/run_alt11_screen{,_realistic}.sh` + probe scripts — two 120-point functional grids (tt/ss/ff/sf/fs × 3.0/3.3/3.6V × 8 input points, polarity-aware verdicts). **Realistic drive (VIN2=VDD/2 as in the SAR): all 15 rows PASS over the full 3–97%·VDD input range** — correct decision for every input the ADC can present. Pure-Vcm stress grid documents the NMOS-pair dead zone (wrong-polarity, deterministic) below ~0.7–1.3V — unreachable in the ADC, but a hard limit for any reuse. The realistic screen caught a real bug: ss/3.0V failed low inputs at CKL=2.8n (mid-rail drive too weak for the strobe) → window edges measured (floor 2.8–2.9n, FF ceiling ≈3.25n) → **CKL=3.1n**, biased away from the wrong-answer edge. MC: **TT N=200 σ=1.14 mV; worst survivor ss/125°C/3.0V N=100 σ=1.29 mV** — both ≥36% under the 2 mV target. Full narrative + window map: `comparator/WORKLOG.md` 2026-08-05. |
| COMP-ALT-12 | Top-level two-stage layout (preamp ↔ latch interface) | Luc | 🟢 **Complete (2026-08-06)** | `comparator_alt/layout/comparator_2stage.gds` — vertical stack, mirror-symmetric DIP1/DIP2 Metal2 twins (DIP1→VIN1 order preserved), equal-width clock crossings, double-sided VDD/VSS straps. **Polarity verified by simulation on the extracted netlist: PASS both directions** — this check caught a swapped CK/CKL label pair that LVS had passed. Details: `comparator/WORKLOG.md` 2026-08-06. |
| COMP-ALT-13 | Two-stage sub-block DRC + LVS clean | Luc | 🟢 **Complete (2026-08-06)** | DRC clean; flat LVS **"netlists match"** vs `comparator_alt/layout/comparator_2stage.spice` (assembled from the verified per-cell references). Extracted netlist: `comparator_alt/layout/comparator_2stage_extracted.cir`. |
| COMP-ALT-14 | Post-layout extraction (PEX) corner simulation | Luc | ⚪ Not Started | Mirrors COMP-9 for this topology. Re-confirm offset **and** delay with extracted parasitics — the preamp's gain is `g_m/C_L`-dependent, so output-node parasitics directly reduce measured gain, and per COMP-ALT-6 a gain change moves the CKL optimum. |
| COMP-ALT-15 | v2 input pair — common-centroid / interdigitated array | Luc | ⚪ Not Started | The `m=16` structure was chosen to make this a drop-in. Addresses **systematic** process gradients, a different mechanism from Pelgrom's random term — and at 64 µm² the random term is now small enough that the gradient term may be comparable. Directly answers the reviewer's open item on common-centroid layout. |

---

## 7 · Automation & Reproducibility Suite (REP)

| ID | Task | Owner | Status | Artifact |
|----|------|-------|--------|----------|
| REP-1 | Python script: parse ngspice `.raw` → DNL/INL | Bruno | 🟢 Complete | `designs/scripts/extract_dnl_inl.py` — reads the `tb_inl_dnl.sch` transient CSV, reports FS span, DNL, INL (endpoint + best-fit), monotonicity/missing-code checks, writes the DNL/INL-vs-code figures. |
| REP-2 | Python script: FFT spectrum → ENOB / SNDR | Bruno | 🟢 Complete (2026-07-31) | `designs/scripts/calc_enob.py` — coherent-FFT SNDR/ENOB with two modes: direct code-series FFT (future silicon captures) and **static-transfer-projected**: reconstructs transition thresholds from the Gate-3 code-center sweep, applies them to a coherent sine, FFTs. Datasheet run (0.65–3.25 V sine = 79% FS, N=8192): **SNDR 45.3 dB → ENOB 7.23 bits** (static figure; no-S/H BW caveat documented). |
| REP-3 | Master simulation runner script | Bruno | 🟢 Complete (2026-07-31) | `designs/scripts/run_all_sims.sh` — regenerates all generated layouts + refs, runs the full DRC/LVS suite (5 blocks + chip + density + antenna), PASS/FAIL summary; Gate-3 256-run sweep behind `--full`. Validated end-to-end: **ALL CHECKS PASS**. Gotcha encoded: `run_dac_{drc,lvs}.sh` cd into the run dir — every path they receive must be absolute. |
| REP-4 | Reproducibility environment doc | Bruno | 🟢 Complete (2026-07-31) | `REPRODUCIBILITY.md` — container setup (HOME/USER/PATH workarounds), non-negotiable sim settings table, per-gate re-run commands, artifact/reference table, datasheet lines. |
| REP-5 | CI library-check passes on `main` branch | Bruno | 🟢 Complete (2026-07-31) | `.github/workflows/library_check.yml` + `designs/scripts/ci_library_check.py` — plain-runner scope (pip klayout, no PDK): GDS integrity/topcell/bbox for all 6 signed-off layouts, LVS-reference device-count guards (X-call sneaking in changes the count), ENOB ≥ 7.0 regression, script compile. Validated green locally; full DRC/LVS stays the container flow per REPRODUCIBILITY.md. |

---

## Review Feedback & Open Items

> From the mid-project schematic review (Reviewer: Saroj Rout — **Total 8/20, Conditional Go**). Terms below are defined in **[docs/adc_glossary.md](docs/adc_glossary.md)**.

| Item | Status |
|------|--------|
| Define measurable target specs: Resolution, ENOB, conversion rate, DNL, INL | 🟢 Done (2026-07-31) — 8-bit, FS 3.3 V, 1 LSB 12.9 mV, DNL/INL < 0.5 LSB (Gates 3/4 PASS), **ENOB 7.23 bits / SNDR 45.3 dB** (static-projected, `calc_enob.py`), **833 kS/s @ 10 MHz** (12 CLK/conversion). Pending only team ratification of the last two numbers. |
| S/H simulation must load worst-case DAC switch config + comparator input cap, not an ideal/light load | 🟢 Done — `tb_sample_hold.sch` loads the full 12.77 pF array + 20 fF comparator cap; full-range acquisition PASS, worst-corner error <0.02 mV. |
| DAC capacitor sizing should be justified via kT/C noise budget, not just "C_u ≥ 50 fF" as a guess | 🟢 Done — kT/C ≈ 18 µV rms for C ≈ 12.75 pF, ~360× below 0.5 LSB. C_u is matching-limited, not noise-limited. |
| Avoid a bootstrap switch for the 8-bit DAC specifically — a sized transmission gate is sufficient and cheaper on schedule | 🟢 Addressed — array switches are per-bit sized NMOS pass-gates; the single full-range sample switch is a sized transmission gate. No bootstrap anywhere. Emily's bootstrap was evaluated and rejected. |
| Comparator offset should be reduced via proper buffer/latch sizing (gm/Id-annotated), not brute-force area scaling | 🟢 Addressed — COMP-5 already treats offset as a calibratable DC shift, no upsizing done |
| Foundry mismatch data assumes perfect common-centroid layout; current comparator layout will have worse real offset than MC predicts | ⚪ Not accounted for in COMP layout yet |
| Keep an overall project tracker (this file) up to date across all blocks | 🟢 Maintained — refreshed 2026-07-30 (SAR block closed end-to-end, COMP-ALT relocated to `comparator_alt/`, critical path updated below) |

---

## What's Left — Critical Path (as of 2026-07-30)

All three sub-blocks (DAC, Comparator, SAR_LOGIC) are individually complete.
Everything remaining is top-level integration.

| # | Item | Block | Blocker / Why it's next |
|---|------|-------|-------------------------|
| 1 | ~~Pin-contract table (INT-2)~~ | INT | 🟢 Done 2026-07-30 — `docs/pin_contracts.md`, conversion scheme decided (DAC as VDAC generator; see INT-2 row). |
| 2 | ~~Sync `strongarm.sch` (COMP-10) + create `sar_logic.sym`~~ | COMP/SAR | 🟢 Done 2026-07-30 — COMP-10 verified by netlist diff; `sar_logic/sar_designs/sar_logic.sym` created, instance pin order verified = subckt port order. |
| 3 | ~~Stitch ADC top + integration TB (INT-3/4/5)~~ | INT | 🟢 Done 2026-07-30 — `adc_top/` (see INT-3/4/5 rows). |
| 4 | ~~**Gate 3 at ADC level** (INT-6)~~ | INT | 🟢 **PASS 2026-07-31** over VIN 0.50–3.29 V (see INT-6 row). |
| 5 | ~~**Gate 4** full corner sweep (INT-7)~~ | INT | 🟢 **PASS 2026-07-31** (see INT-7 row). |
| 6 | ~~**Gate 5** chip-level DRC/LVS (INT-8)~~ | INT | 🟢 **PASS 2026-07-31** — chip block assembled + signed off (DRC 0/660, LVS 862/862, density + antenna clean); SAR folded 3-row, glue laid out, COMP-11 taps + dummy fill included. See INT-8 row. **All gates closed — the ADC block is tapeout-ready.** Remaining team-side: padring hookup at chipathon integration (13 labeled M5 south-edge pins are the contract), ENOB/sample-rate spec ratification, REP-2..5. |

**Not on the critical path** (do when convenient): COMP-9 full-RC PEX (5.4x
delay margin makes it confirmation, not risk); COMP-ALT-7/10/11/12/13/14/15
(Luc's double-tail backup, for the paper); REP-2 `calc_enob.py`; REP-3 master
sim runner; REP-4 `REPRODUCIBILITY.md`; REP-5 CI on `main`.

**Specs — now defined (2026-07-31, pending team ratification):** target
**ENOB ≥ 7.2 bits** (static-transfer-projected, SNDR 45.3 dB at 79% FS
swing — `calc_enob.py` on the Gate-3 sweep; dynamic use limited by the
no-S/H BW ≈ 1.5 kHz for full-swing sines) and **sample rate 833 kS/s @
10 MHz CLK** (12 CLK/conversion). Everything else was already fixed
(FS = 3.3 V, 1 LSB = 12.9 mV, DNL/INL < 0.5 LSB, input range
0.65–3.25 V quoted / 0.62–3.29 V guaranteed).

---

## Verification Gates Summary

| Gate | Criterion | Block | Status |
|------|-----------|-------|--------|
| Gate 1 | σ_offset characterized + delay < 2 ns @ TT (MC N≥100) | Comparator | 🟡 |
| Gate 2 | DAC settling ≤ 0.5 LSB within 40 ns @ TT | Cap DAC | 🟢 PASS, post VREF=VDD rework 2026-07-18 (TT 2.61ns, worst corner SS/125°C/2.97V 3.86 ns / 36.1 ns margin — see `VERIFICATION_PLAN.md`) |
| Gate 3 | Top-level DNL/INL < 0.5 LSB @ TT corner | Integration | 🟢 **ADC-level PASS 2026-07-31 over VIN 0.50–3.29 V** (256-code sweep, monotonic, all codes within 1 LSB = calibratable offset; below 0.5 V dead — comparator low-Vcm limit; see INT-6). DAC-only nominal sweep PASS post VREF=VDD rework (FS span=3293mV=99.8% of 3.3V, max\|DNL\|=0.002 LSB, max\|INL\|=0.004 LSB, see DAC-9); cap-mismatch (the real linearity limit) also **PASSES at Cu=50fF (≥99.9997% yield, corrected local-mismatch model, see DAC-9b — LSB-based conclusion unaffected by the rework)** — no upsizing needed, common-centroid layout (DAC-5) still required for systematic-gradient cancellation. Full ADC-level (with comparator + sequencing) integration still ⚪ |
| Gate 4 | Full corner sweep (FF/SS/SF/FS) passes spec | Integration | 🟢 **PASS 2026-07-31** — all 4 corners monotonic, within 1 LSB above the corner's dead zone; guaranteed range 0.62–3.29 V (see INT-7, `adc_top/sim/corners_report.md`) |
| Gate 5 | DRC clean + LVS clean → tapeout sign-off | Integration | 🟢 **CHIP-LEVEL PASS 2026-07-31** — `adc_top/layout/adc_chip_top.gds`: main DRC **0/660**, LVS vs native-element reference **"Netlists match" 862/862 devices**, density (post dummy-fill) and antenna checks clean, COMP-11 taps in. (DAC block sign-off 2026-07-19: DRC 0/660, LVS 377/377 — now subsumed.) See INT-8. |

**DAC physical integration (2026-07-19, complete):** top-level routing is complete and the block is DRC/LVS-clean against the real reference netlist (`dac/layout/dac_top_ref.spice`, 12 ports, 377 devices: 324 cap_mim + 26 nfet + 27 pfet). Final pass tied the TG PMOS nwell-tap (DVDD) to the VDD spine and strapped the dummy-ring M3 GND frame to the "0" spine (reference dummies restored to `0`, per DAC-6 intent). `designs/scripts/check_all_bits.py` reports all global and per-bit signal/supply continuity PASS (incl. TG-bulk↔VDD and dummy-ring↔0) and all **30** tracked nets pairwise distinct (**0/435 merges**); pristine `cap_array` remains 8 distinct rails. Full-block variant-D DRC **0/660**; LVS **"Congratulations! Netlists match"**. The DAC block is tapeout-ready.
